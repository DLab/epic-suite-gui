import ModelBuilder from "components/new-model/ModelBuilder";

const ParametersAccordion = () => {
    return (
        <>
            <ModelBuilder isEditing setIsEditing={() => {}} />
        </>
    );
};

export default ParametersAccordion;
